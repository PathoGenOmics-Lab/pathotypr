<!--
Thanks for sending a pull request. Fill in what applies and delete what does
not. A short PR needs a short description; nobody is asking you to write an
essay for a typo fix.
-->

## What this changes

<!-- One or two sentences. What is different after this is merged? -->

## Why

<!-- The problem it solves. Link an issue with "Closes #123" if there is one. -->

## How it was verified

<!--
The important part. Not "it should work", but what you actually ran and what it
printed. For example: the command and its output, the test you added and the
fact that it fails without the fix, or the accession you re-classified and the
call you got before and after.
-->

## Checklist

- [ ] `cargo test` passes.
- [ ] `cargo check` is clean.
- [ ] New behaviour has a test, or I have said why it does not.
- [ ] Documentation under `docs/` is updated if the change is user-visible.
- [ ] Nothing in the diff hard-codes an assumption about one organism.
