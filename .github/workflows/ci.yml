name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-22.04
    # Measured at about 5 minutes. The ceiling exists so a hung apt or a cold
    # cache fails in minutes instead of occupying a runner for the 6 hour
    # default, which is what happened on 2026-08-19.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable

      - name: Install build dependencies
        timeout-minutes: 10
        run: |
          # apt-get update queries about thirty repositories and is where this
          # step hangs: on 2026-08-19 it got through nine of them in ten minutes
          # and never reached the install. The runner image already ships
          # package lists, so install from those and only refresh them if a
          # package cannot be found.
          sudo apt-get install -y cmake ||
            { sudo apt-get update && sudo apt-get install -y cmake; }

      - name: Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Check (core)
        run: cargo check -p pathotypr-core

      - name: Test (core)
        run: cargo test -p pathotypr-core

      - name: Clippy (core)
        run: cargo clippy -p pathotypr-core --lib -- -D warnings 2>/dev/null || true

      - name: Format check
        run: cargo fmt --check -p pathotypr-core 2>/dev/null || true

  # The core job never builds the desktop backend, so nothing used to compile
  # reqwest, the HTTP client or the TLS stack on a pull request.
  gui:
    name: GUI (Linux)
    runs-on: ubuntu-22.04
    # Measured at about 3 minutes.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable

      # cmake for pathotypr-core; the rest is the GTK and WebKit stack the
      # Tauri backend links against, matching what release.yml installs.
      - name: Install build dependencies
        timeout-minutes: 10
        run: |
          # apt-get update queries about thirty repositories and is where this
          # step hangs: on 2026-08-19 it got through nine of them in ten minutes
          # and never reached the install. The runner image already ships
          # package lists, so install from those and only refresh them if a
          # package cannot be found.
          PKGS="cmake pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev
                libayatana-appindicator3-dev librsvg2-dev"
          sudo apt-get install -y $PKGS ||
            { sudo apt-get update && sudo apt-get install -y $PKGS; }

      - name: Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Check (gui)
        run: cargo check -p pathotypr-gui

      # reqwest is pinned to rustls so the Linux build carries no OpenSSL. A
      # dependency bump could quietly pull native-tls back in, and openssl
      # with it, without anything else here noticing.
      - name: Assert the Linux build carries no OpenSSL
        shell: bash
        run: |
          set -euo pipefail
          tree=$(cargo tree -p pathotypr-gui \
            --target x86_64-unknown-linux-gnu --edges normal --prefix none)
          if grep -qE '^(openssl|native-tls) v' <<<"$tree"; then
            echo "::error::native-tls or openssl is back in the Linux dependency graph"
            grep -E '^(openssl|native-tls) v' <<<"$tree" | sort -u
            exit 1
          fi
          echo "No openssl and no native-tls in the Linux dependency graph."
